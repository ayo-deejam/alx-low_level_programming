#include <stdio.h>
#include <string.h>
/**
 * * main - Entry point
 *
 * * Description: 'this programme prints to the standard utput using puts function'
 *
 * * Return: Always 0 (Success)
 */
int main(void)
{
char string[] = '"Programming is like building a multilingual puzzle';
puts(string\n);
return (0);
}
